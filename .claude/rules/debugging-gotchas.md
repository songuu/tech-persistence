# 踩坑记录 & 调试经验

> 按严重程度排列。高置信度的调试本能 (/instinct-status domain:debugging) 毕业后写入此处。
> 由 /learn 和 /debug-journal 自动追加。

## CRITICAL — 必须知道

## HIGH — 容易踩到

- [2026-06-01] [telemetry, observe, mcp, tool-name-normalize] **从 observations 按工具名 filter 前必须先看 `normalizeToolName` 的变换——它剥 `mcp__`/`functions.` 前缀，用客户端原始名 `mcp__*` 匹配必然 0 命中（静默失效，比报错更隐蔽）**。缺陷 B（MCP 主动检索 measure）要数 `tp_memory_*` 调用，roadmap task 骨架初稿写「`tool` 匹配 `mcp__*memory*`」。但 `scripts/lib/memory-v5.js` 的 `normalizeToolName` 做 `.replace(/^functions\./,'').replace(/^mcp__/,'').slice(0,80)`——MCP 调用 `mcp__tech-persistence-memory__tp_memory_search` 进 `observations.jsonl` 后记录为 `tech-persistence-memory__tp_memory_search`。`mcp__*` filter 匹配 0 条 → metric 永远 0（fail-open 到假信号）。识别信号：(1) 新增的「按工具名统计」字段实测恒为 0；(2) grep observations 的 `tool` 字段看不到任何 `mcp__` 前缀。修复：按工具名**后缀**匹配（`/tp_memory_/`），不依赖被剥的 `mcp__` 前缀也不依赖 server 名（更稳）。`scripts/observe.js` matcher `*` 确实捕获 MCP 调用，`tp_memory_` 子串 normalize 后存活。另：`observe.js` 每次调用写 pre+post 两条 observation，**计数类**统计须只数 `phase==='post'` 否则翻倍 2×。规则：**写"按 observation 字段统计"的代码前，先读 `normalizeToolName` / `redactObservation` 看字段被怎么变换过**，别用上游原始值假设。元本能 [[documented-claim-vs-code-reality-drift]]。见 [[ADR-022]]。
- [2026-05-29] [codex-plugin, build, scripts-lib, require-closure] **`copyHookLibs` 只填 `hooks/lib`+`mcp/lib`，不填 plugin `scripts/lib`——`scripts/` 下 utility 脚本新增 `require('./lib/*')` 会让 plugin 副本运行时 `Cannot find module`**。已有的下方 hook-lib 注只覆盖 hook 脚本；utility 脚本（`agent-orchestrator.js` / `skill-eval-*.js` / `skill-traces.js`）是另一条解析路径：它们的 `require('./lib/x')` 相对脚本自身解析到 `plugins/tech-persistence/scripts/lib/x.js`，而 `build-codex-plugin.js::copyUtilityScripts` 此前只复制顶层脚本 + `copyAgentOrchestratorSubmodules`（`scripts/agent-orchestrator/` 子目录），**从不复制 `scripts/lib`**。A3 clarification channel 给既有 `agent-orchestrator.js` 引入**首个** `./lib` 依赖（`require('./lib/clarifications')`，此前整条闭包零 `./lib` 依赖、全部已复制）→ plugin 副本的 `/agent-loop` 一并崩（P0 静默回归）。B1/B2/B3 的 3 个新 CLI 同样全断（`./lib/<x>` + `./lib/runtime-paths` + 传递 `./lib/redaction`）。为何漏掉：(1) ADR-016/017/018/019 都写「lib 经 copyHookLibs glob 自动复制」——**错的**，copyHookLibs 目标是 hooks/mcp 不是 scripts；(2) `validate-codex-plugin.js` 的 `validateLocalRequireClosure` 只查 hook 脚本，utility 脚本只断言「is a file」→ 盲区；(3) 测试只跑 root `scripts/`（有 `scripts/lib`），从不跑 plugin 副本；(4) pre-commit sha256 sync 比对源↔副本字节，副本是忠实拷贝、只缺传递依赖，sha256 查不出。修复：`copyUtilityScripts` 末尾 `copyHookLibs(path.join(pluginRoot,'scripts'))` 填 `scripts/lib`；`validate-codex-plugin.js` 对 utility 脚本加 `validateLocalRequireClosure` 护栏（删 lib 跑 validate 必 fail 的 break-impl 负样本已验证）。规则：**任何 `scripts/` 下被 `copyUtilityScripts` 复制的脚本新增 `require('./lib/*')`，必须确认 plugin `scripts/lib` 被填 + validator 闭包护栏覆盖**。元本能 [[documented-claim-vs-code-reality-drift]] + [[reuse-existing-infra-before-building-new]]。见 [[ADR-020]]。
- [2026-05-22] [propagate, build, multi-copy-sync] **改 `user-level/commands/*.md` 源后 `propagate-command-changes.js` 单跑不够，必须再跑 `build-codex-plugin.js`**。`scripts/pre-commit-check.js::checkPropagateSync` 用 `build.transform`（来自 `build-codex-plugin.js`）比对 sha256，propagate 用的 `injectIntoSkillWrapper` transform **输出可能不一致**（plugin skill wrapper 含额外 build step 注入）。只跑 propagate → pre-commit 报 `× skill wrapper mismatch` 把 4 ok 行直接打脸。固化命令序列：`node scripts/propagate-command-changes.js <cmd>... && node plugins/tech-persistence/scripts/build-codex-plugin.js && node scripts/validate-codex-plugin.js && node scripts/pre-commit-check.js`。**不要**只跑 propagate 就 commit。未来重构方向：让 propagate 也调用 build.transform 写 plugin skill (build 是 canonical) 或让 pre-commit 用 propagate 的 transform。本能 [[propagate-needs-build-step-for-skill-wrapper]]。
- [2026-05-22] [propagate, codex-regex, runtime-label-collision] **源文档用 `Claude Code 端` / `Codex 端` 标签做 runtime 区分会被 codex regex 撞车合并**。`propagate-command-changes.js` 的 codex regex 把 `Claude Code` → `Codex`、`Claude` → `Codex` 全替换；源里两段标题 `#### Claude Code 端 prompt 模板` + `#### Codex 端 prompt 模板` 经 propagate 后 codex 副本两段都变成 `#### Codex 端 prompt 模板`——runtime 区分语义丢失，文中 "Claude Code 端调用...Codex 端内联" 变成 "Codex 端调用...Codex 端内联" 自相矛盾。规则：跨 runtime 区分标签必须用 codex regex 不替换的 idiom（`Spawn-capable runtime` / `Inline-fallback runtime` / `Agent-spawn-capable` / `single-context-only`）。grep codex 副本验证不撞车后才能 commit。本能 [[codex-regex-sync-needs-runtime-neutral-idiom]]。

- [2026-05-15] [memory, dual-system, tech-persistence-only] **TP 同时跑两套 memory 系统，路径键完全不同**。`~/.claude/projects/C--<cwdpath>/memory/`（Claude Code core 的 auto memory，**用户写**的 `feedback_*` / `user_*` 都在这）vs `~/.claude/homunculus/projects/<gitRemoteHash>/memory/`（Tech-persistence v5，**hook 写 + hook 读**）。两套**路径键不同**（cwd path escape vs git remote hash）+ **注入机制不同**（Claude Code core SessionStart vs `inject-context.js` hook）+ **runtime 可达性不同**（auto memory 只 Claude Code 可见，v5 经 [[ADR-008]] compatReadDirs 双 runtime 可见）。实施 Persona 顶层维度时第一直觉写到 auto-memory dir → `inject-context.js` dogfood 发现 Persona 段缺失 → 才发现两套并存。规则：实现 memory 相关功能前先 `grep` 目标 hook 读取的路径常量，**不能凭"`feedback_*` 都在那个目录"假设新文件也写那里**。设计文档写 "memory/" 必须明示是 auto-memory 还是 v5。**额外暗示**：所有现有 `feedback_*`/`user_*` 在 Codex 端通过 v5 不可见（因为它们在 auto-memory dir 不在 v5 dir）——这是已存在但此前未发现的 [[ADR-011]] multi-runtime parity 违反，[[ADR-015]] 标记为 P1 backlog。元本能 [[reuse-existing-infra-before-building-new]]（复用 `compatReadDirs` + `detectProjectIdentity` 时未及时验证目标 dir 是否存在已写入文件）。
- [2026-05-14] [claude-code, plugin, skill-scanner, deprecation] **Claude Code 2.x skill scanner 对 `~/.claude/skills/<dir>/` 目录名后缀无过滤**。第一直觉 `mv skill-foo skill-foo.deprecated-2026-05-14` 想软删除，但 scanner 把 `.deprecated-*` / `.legacy` / `.old` 等任何后缀都注册为合法 skill name，仍出现在 skill picker / system-reminder 列表。R5 处置时实测：rename 5 个 TP 重复 skill 后 system-reminder 立刻列出 `continuous-learning.deprecated-2026-05-14: 基于 Hook 的持续自学习系统...` 等 5 项作为有效 skill。规则：deprecating user-level skill 目录必须**移出 `~/.claude/skills/` 子树**，例如 `mv ~/.claude/skills/<name> ~/.claude-backups/<name>`，而不是改名。同样适用于 `~/.claude/commands/`（虽然 2.x 已废弃但回退兼容仍可能扫到）。跨系统泛化：依赖目录扫描注册的系统（VSCode extension / Vim plugin / npm node_modules）都是 inclusive 不是 exclusive，**deprecation 必须靠物理移走**。元本能 [[claude-skills-name-suffix-regex-permissive]]。
- [2026-05-14] [bash, set-e, pipefail, ls, glob] **`set -euo pipefail` + `ls $glob 2>/dev/null` 无匹配 → exit=2 杀整个函数**。在 `install.sh::safe_copy` 加 retention 时写 `existing=$(ls -1t "$dst.bak."* 2>/dev/null | tail -n +4)`，期望无匹配返空字符串。**实际**：ls 无匹配 exit=2，stderr 被吞但 stdout 空 + exit=2 通过 pipefail 提到管道顶层，命令替换 `$(...)` 继承 exit=2，set -e 立刻终止函数。最隐蔽的是没有任何报错（stderr 被 `2>/dev/null` 吃了），用户只看到 install 脚本静默退出。规则：`$(ls $glob 2>/dev/null | ...)` 必须以 `|| true` 结尾兜底；或改用 `compgen -G` / `find -name`（无匹配 exit=0）。验证 smoke 必须包含 S2「0 个匹配 → set -e 不杀」负样本，单纯有匹配的 case 不能证明兼容性。元本能 [[bash-pipefail-vs-ls-no-match]]。
- [2026-05-12] [hash, cross-platform, lf, crlf] **`sha256(fileA) vs sha256(fileB)` 跨副本 enforcement 必须先 LF-normalize**。本项目 `scripts/pre-commit-check.js::sha256OfFile` 第一版直接 `update(fs.readFileSync(p))` raw byte。但 `build-codex-plugin.js::copyTextFile` 内部跑 `normalizeLf(content.replace(/\r\n/g, '\n'))`——plugin 副本永远是 LF。如果 Windows / `core.autocrlf=true` / 某 editor 把源以 CRLF 保存，source sha ≠ plugin sha 永不平等，repair command "run build" 解不开（build 把 plugin 写成 LF 但源仍 CRLF），用户唯一出口是 `--no-verify`。规则：任何「源副本 sha 比对」类 enforcement，hash 之前必须 normalize 两端到同一 line ending（推荐 LF，`update(text.replace(/\r\n/g,'\n'), 'utf8')`）。验证 smoke：必须有一个 scenario「源 CRLF + 副本 LF → exit 0」（`scripts/smoke-pre-commit.js` S10 是参考实现）。元本能 [[cross-platform-sha-needs-lf-normalize]]。
- [2026-05-12] [build, enforcement, contract-pair] **非递归 build + 不匹配的 detection regex = 静默 incomplete bundle**。本项目 `copyAgentOrchestratorSubmodules`（plugins/.../build-codex-plugin.js:332）只 `fs.readdirSync` 顶层不递归。如果有人加 `scripts/agent-orchestrator/providers/claude.js`，build 静默跳过、pre-commit regex `[^/]+\.js` 也不匹配嵌套，源文件被 git track 但 plugin bundle 缺少。运行时 `require` 报 module not found，根因在 build 设计。规则：(a) 非递归 build / codegen 必须配 detection 端拒绝不符合 flat invariant 的输入；(b) detection 在源 dir 扫描时遇到子目录立刻报 mismatch 而非过滤跳过。本项目实现：`checkOrchestratorSync` 用 `withFileTypes: true` 遇到 dir 即 push mismatch，reason "nested directory ... build is non-recursive"。smoke S12 验证。
- [2026-05-12] [audit, documentation, drift] **「文档声称已修复 vs 代码实际未落地」是高频回归源**。2026-05-12 `/agent-loop` follow-up audit 复查 `docs/solutions/2026-05-09-agent-loop-caveman-audit.md` 中的 8 条 fix，**4 条 grep 不到**对应代码（partial / still-open），包括 ADR-008 明示要做的 `loadRun` defensive default、validation 顶层时间戳、follow-up-task 行式 fallback、runResume 拆分 gate。本能 [[state-file-backward-compat]] 本身就是该现象的活样本——本能描述了应该做什么，但代码中并未落地。识别信号：(1) solution doc 用「已修复 / 已实施 / closed」字样但 grep 关键标识符 0 命中；(2) sprint plan `status: implemented` 但 P0/P1 表格全是占位行；(3) 本能 doc 写「应该在 X 入口 default」但读 X 入口代码无对应分支。规则：对所有「已修」claim 必须 grep / Read 验证，**信文档前先信代码**；分类用 4 档（closed / partial / still-open / wont-fix-now），partial 是这条规则的核心收益——避免半修被误归已修。修复方向永远是「升级 enforcement」（pre-commit-check 加 sha256 / test 加 assertion / lint 加规则），不是「记得多检查」。元本能 [[documented-claim-vs-code-reality-drift]]。
- [2026-05-12] [testing, enforcement, dogfood] **新 enforcement 在「当前态已全部合规」时必须主动制造负样本验证**。2026-05-12 audit 发现 `scripts/agent-orchestrator*` 13 个文件根/plugin 副本 sha256 全 EQUAL — 这意味着任何新加的 enforcement hook 会 trivially pass 所有当前文件，**单纯跑 pre-commit-check 不能证明 hook 真在拒**。规则：写新 enforcement 后必须做 3 步 smoke：(1) 当前态运行应 pass；(2) **临时制造不一致**（改一个 plugin 副本 1 字符）→ 再跑必须 fail，且错误消息指出具体不一致文件；(3) 恢复一致 → 再跑必须 pass。少了 step 2 = enforcement 上线但实际从未被验证过（fail-open 风险）。本规则与 [[ADR-013]] dogfood 边界产物枚举互补：ADR-013 防"误拒既存"，本条防"漏拒新违规"。
- [2026-05-12] [hooks, shell-mismatch, windows] **Claude Code 在 Windows 上通过 Git Bash 执行 hook command，不是 cmd.exe**（**已踩 2 次** — 2026-04-09 初次发现并修，后重构回归，2026-05-12 二次修复）。`~/.claude/settings.json` 里的 hook command 若写 cmd 风格 `2>nul || exit /b 0`，在 bash 里 `2>nul` 会被解释为「stderr 重定向到当前目录下名为 `nul` 的真实文件」（每次 hook 触发都创建/覆盖 0 字节文件），`exit /b 0` 是无效语法但因 `||` 短路从未执行所以不可见。证据：(1) `nul` 是 Windows 保留设备名，cmd.exe 永远无法在文件系统创建同名文件；能在仓库根目录看到 `?? nul` 就反证执行环境是 bash；(2) 同 settings 的 statusLine 也是 `bash -c '...'`。规则：hook command 永远用 POSIX 语法 `2>/dev/null || true`——bash 直接生效，cmd 路径下 `/dev/null` 虽是字面但 `|| true` 短路兜底。回归预防：纯文档协议 enforcement 已证明抵不住重构；下一道防线应该是 **CI/test 检查**，对所有安装产物（`install.ps1` 生成的 settings、`merge-claude-settings-hooks.js` 输出）断言 hook command 不含 `2>nul` / `exit /b`。本项目污染源（2026-05-12 修复）：`install.ps1:60-63` 模板 + `scripts/merge-claude-settings-hooks.js:61` windows 分支 + 旧 solution `docs/solutions/2026-04-09-obsidian-integration.md` 把错误经验记录在案（已加 errata）。
- [2026-05-12] [enforcement, dogfood] **新 enforcement（pre-commit / lint / CI reject）合并前必须枚举 ≥3 个已有同类产物验证不被误拒**。本仓库刚加 pre-commit hook 时，dogfood 只测了"我刚写的 plan 通过"+"破坏一次再恢复"，**没枚举本仓库 6+ 个无 frontmatter 旧 plan**。Phase 4 reviewer 才发现：hook 上线第一天就阻塞合法的旧 plan 修订，用户必须 `--no-verify` 绕过——enforcement 最差启动状态。修复：grandfather signal 改用 filename date（`PLAN_PATH_RE` 强制 capture，独立于 frontmatter），一次性解决无-FM / CRLF / 不可解析 created 三个失效。规则上升为 [[ADR-013]]：plan 阶段「关键假设验证」段必须含「会拒绝哪些现有产物」枚举。
- [2026-05-12] [metadata, signal-source] **filename 比 frontmatter 鲁棒——优先用 git-enforced metadata**。当 metadata 既在 filename 也在 frontmatter（plan / solution / changelog），filename 是 path-regex 强制（不匹配 pattern 根本不进 list），frontmatter 是 author-discipline-enforced。Grandfather / 类型判定 / 时间窗口类决策应优先 filename。Frontmatter 只适合可变状态（status / tags / aliases），不适合不变事实（created / type / date）。
- [2026-05-12] [tests, fail-open] **fail-open 系统的"成功"测试必须额外断言 fail-open marker 不在输出中**。任何 hook / lint / CI 用 exit 0 同时承担"通过"和"已禁用"两个语义时，单测 `assert(exit === 0)` 不区分这两种情况——hook 整体失效时所有"通过"测试还会绿。规则：在所有 pass 类 scenario 末尾加 `assert(!/<fail-open-marker>/.test(stderr))`。本项目 fail-open marker 例子：`hook 内部异常已忽略` / `fail-open 放行` / `[hook] failed:`。
- [2026-05-12] [error-message, ux] **error 输出中的"修复方式"必须 copy-paste runnable，不能含未替换占位符**。`<cmd>...`、`[--flags <value>]` 风格的占位符让用户每次 failure 都做"读 mismatch → 提取 basename → 区分类型 → 拼参数"4 步翻译。规则：error formatter 从 finding records 派生具体值（cmd 名 / rule 名 / 文件路径）；唯一例外是"模板让用户填空"（如 plan-lint 给的「关键假设验证」段模板），此时模板格式要清晰、有 `<占位>` 标记。
- [2026-05-11] [claude-code, hooks, installer] **已有 `settings.json` 缺 hook 时不能只提示手动合并**。命令、rules、hook 脚本都复制成功但 settings 没有 `SessionStart` / `PreToolUse` / `PostToolUse` / `Stop`，会形成“安装看似完成、学习层实际不跑”的静默失效。安装器必须用 JSON 结构化 merge 保留现有配置并补齐 hook；验证必须跑 `node scripts/validate-claude-install.js --user` 和 `--project`。
- [2026-05-11] [agent-loop, provider-errors] **`claude -p --output-format json` 的错误内容写在 stdout，不是 stderr**。401 / 429 / quota 等场景 claude CLI 写 `{is_error:true, api_error_status:401, result:"Failed to authenticate. API Error: 401 Invalid bearer token"}` 到 **stdout**，stderr 留空，进程 exit code = 1。当时 orchestrator 报错只指向 stderr（`see stderr.log`），用户打开看到空文件，会以为是代码 bug，根因被掩埋。修复：`runProcess` 在 status≠0 时先尝试解析 stdout 的 envelope（`is_error / api_error_status / result`）并把人类可读的 `result` 字段拼进错误消息；报错路径同时给出 stdout 和 stderr。预防：（1）新增 provider 类调用一律走 `runProcess`（中央化错误处理），不要直接 `spawnSync` 后自己抛 "see stderr"；（2）`doctor --probe` 用最小调用真打一次 claude / codex，提前暴露认证 / token 失效；（3）跨副本同步必须 `node plugins/tech-persistence/scripts/build-codex-plugin.js` + `node scripts/validate-codex-plugin.js`，agent-orchestrator.js 是 git tracked 派生文件。
- Hook 脚本一旦新增 `scripts/lib/*` 依赖，必须同步更新安装脚本和 Codex plugin 构建脚本；否则用户环境中的 hook 会因 `Cannot find module './lib/...'` 静默失效。验证时至少跑 `node plugins/tech-persistence/scripts/build-codex-plugin.js`、`node scripts/validate-codex-plugin.js` 和临时 `TECH_PERSISTENCE_HOME` smoke test。
- Memory v5 不能用 first-hit fallback 读取 `MEMORY.md`；Claude Code 和 Codex 默认目录都可能有 durable topic notes。SessionStart 应合并 `resolveCompatReadDirs()` 下的 topic entries，并用 `node scripts/smoke-memory-parity.js` 验证双向可见。
- [2026-05-09] [docs, dispatch] **CLI 用法 section 列出的子命令必须在执行规则 section 显式分派**。`/agent-loop doctor` 之前被吞进"不是 freeze/resume/status 时 → run --requirement"分支，AI 真把 `doctor` 当成需求字符串创建了新 run。预防：用法表的每行子命令在 dispatch section 都要有对应 case；fallback 条件必须列出全部已知子命令的反集。
- [2026-05-09] [orchestrator, state-migration] **持久化 state 新增字段必须在反序列化入口做 default**。`state.providerRuns.push(...)` 在 v6 → v7 时炸 — 旧 state.json 没有 `providerRuns` 字段，`undefined.push()` 直接 crash。修复：`loadRun()` 入口处统一 `if (!Array.isArray(state.providerRuns)) state.providerRuns = [];`，不要在每个调用点判。
- [2026-05-09] [hooks, observability] **`try { ... } catch {}` 是不可观察的失效**。`caveman-activate.js` 之前吞所有异常返回 0，hook 静默失效用户无感知。规则：hook 不能 crash 主会话，但 catch 块必须 `process.stderr.write('[hook] failed: ${msg}\n')` 至少留下一行日志。
- [2026-05-09] [multi-copy] **git tracked 派生文件必须靠 propagation 脚本同步，不能手工 Edit**。本项目 `.codex/commands/*.md` 是从 `user-level/commands/*.md` 经 `Claude Code → Codex` regex 派生的，被 git tracked。改一次源 → 4 个副本 → 9 个命令 = 36 个手工 Edit，错误率不可控。规则：发现 git tracked 派生文件 → 先找 install/build 脚本里的 transformation 规则 → 写 `scripts/propagate-*.js` 一次性同步。本次产物：`scripts/propagate-command-changes.js`。

- [2026-04-15] [powershell, encoding] **本项目生成的 .ps1 脚本必须带 UTF-8 BOM**
  - 现象：PS 脚本执行时中文变乱码（如 `测试` → `娴嬭瘯`），tokenizer 报 "ExpressionsMustBeFirstInPipeline"、"一元运算符 + 后面缺少表达式"、"语句块中缺少右 }" 等一连串语法错，看似脚本被整体破坏。
  - 根因：中文 Windows 系统 ACP = 936 (GBK)。PowerShell 5.1 对 **无 BOM** 的 .ps1 用系统 ANSI 码页解码。UTF-8 字节被当 GBK 解析后，中文字符变成非法标识符/运算符序列，tokenizer 级联崩塌。**与脚本逻辑无关**，纯编码问题。
  - 快速识别：错误行号集中在含中文的行、并且错误类型是"意外的标记"/"缺少表达式"而非运行时错误 → 99% 是 BOM 问题。
  - 修复：前置 3 字节 `EF BB BF`，PS 5.1 见 BOM 走 UTF-8 路径。bash 一行：`{ printf '\xef\xbb\xbf'; cat file.ps1; } > file.ps1.tmp && mv file.ps1.tmp file.ps1`。
  - 预防：本项目所有 .ps1（install / update / apply-*-delta 系列）生成时必须写 BOM。PowerShell 7+（pwsh）默认 UTF-8，但不能依赖用户已装 PS7——必须向下兼容 PS 5.1。
  - 历史：已踩 2 次。commit b6dc85f 修 `update.ps1`，2026-04-15 又在 `test-strategy-delta/apply-test-delta.ps1` 复现。第三次出现 = 工具链应自动化（脚本生成器强制写 BOM）。

## MEDIUM — 偶尔遇到

## LOW — 边缘情况
