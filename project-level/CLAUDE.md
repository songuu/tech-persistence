# Project instructions for Claude Code

保持本文件短小，只承载项目入口和路由；具体约束按 `.claude/project-standards.json` 渐进读取。

## 事实来源

- 先检查真实依赖清单、workspace 配置、源码入口、测试、构建与部署文件，再描述项目架构。
- 仓库脚本与配置优先于 README、历史说明和占位示例；冲突时报告证据，不静默选择旧文档。
- 不预设包管理器、目录名、框架、数据库或部署平台；只记录当前仓库可验证的事实。

## 工作约定

- 遵循仓库现有边界、命名、错误处理和验证方式，保持修改最小并保留用户未提交内容。
- Bug 修复需要回归覆盖；先运行最窄相关检查，再按风险扩大到类型、测试、lint、构建或端到端。
- 生成文件只通过其权威生成器更新；高风险、不可逆或外部可见操作需要显式门控与恢复方式。
- 分开报告已验证事实、推断、未知项、环境阻塞和外部状态。

## 项目事实（由维护者填写）

- 定位：
- 关键入口：
- 权威命令：
- 当前迭代重点：
