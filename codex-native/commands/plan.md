---
description: "Codex-native plan thin entry point"
---

# /plan

Load exactly one installed Tech Persistence skill whose frontmatter name is
`plan` (normally `$tech-persistence:plan`) and follow it with the user's
remaining text as arguments.

Do not load the legacy generated plan protocol. The selected native skill owns
architecture, task boundaries, risks, and verification strategy.
