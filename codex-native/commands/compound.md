---
description: "Codex-native compound thin entry point"
---

# /compound

Load exactly one installed Tech Persistence skill whose frontmatter name is
`compound` (normally `$tech-persistence:compound`) and follow it with the
user's remaining text as arguments.

Do not load the legacy generated compound protocol. The selected native skill
owns evidence filtering, permission boundaries, knowledge writes, and reporting.
