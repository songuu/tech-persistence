---
description: "Codex-native think thin entry point"
---

# /think

Load exactly one installed Tech Persistence skill whose frontmatter name is
`think` (normally `$tech-persistence:think`) and follow it with the user's
remaining text as arguments.

Do not load the legacy generated think protocol. The selected native skill owns
scope, success criteria, and the decision to hand off to Plan.
